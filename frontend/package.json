{
  "name": "voice-agent-frontend",
  "version": "0.1.0",
  "description": "ローカル音声エージェント フロントエンド（Electron + React）",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently -n renderer,electron -c cyan,yellow \"vite\" \"wait-on http://localhost:5173 && cross-env NODE_ENV=development electron .\"",
    "build": "vite build",
    "start": "electron .",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^33.3.1",
    "vite": "^6.0.7",
    "wait-on": "^8.0.1"
  }
}
